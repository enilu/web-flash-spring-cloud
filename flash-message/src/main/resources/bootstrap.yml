spring:
  cloud:
    config:
      name: flash-message
      label: master
      discovery:
        enabled: true
        service-id: gitConfigServer
eureka:
  client:
    service-url:
      defualtZone: http://localhost:8761/eureka/