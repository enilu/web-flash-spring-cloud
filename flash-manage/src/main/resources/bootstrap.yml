spring:
  cloud:
    config:
      name: flash-manage
      label: master
      discovery:
        enabled: true
        service-id: flash-config-server
eureka:
  client:
    service-url:
      defualtZone: http://localhost:8761/eureka/