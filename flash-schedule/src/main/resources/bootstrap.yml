spring:
  cloud:
    config:
      name: flash-schedule
      label: master
      discovery:
        enabled: true
        service-id: flash-config-server
eureka:
  client:
    service-url:
      defualtZone: http://localhost:8761/eureka/